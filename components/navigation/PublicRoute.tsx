// import { Navigate, Outlet } from "react-router-dom";
import { useAuth } from "../../contexts/AuthContext";

export default function PublicRoute() {
  // const { currentUser } = useAuth();

  // if (currentUser) {
  //   return <Navigate replace to="/my-setup" />;
  // }
  // return <Outlet />;
  return <></>;
}
